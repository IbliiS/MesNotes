<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.baptiste.mesnoteas" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".AccueilActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AjouterNoteActivity" />
        <activity android:name=".AjouterMatiereActivity" />
        <activity android:name=".AjouterMoyenneActivity" />
        <activity android:name=".AjouterAnneeActivity" />

    </application>


</manifest>
